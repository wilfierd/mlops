# Copy to backend.tf and replace <ACCOUNT_ID> with your AWS account id.
# The bucket + lock table are created by infra/bootstrap.
#
# Then run:
#   terraform init -migrate-state
#
# terraform {
#   backend "s3" {
#     bucket         = "llm-chat-tfstate-<ACCOUNT_ID>"
#     key            = "mlops/llm-chat/dev/terraform.tfstate"
#     region         = "us-west-2"
#     dynamodb_table = "llm-chat-tflock"
#     encrypt        = true
#   }
# }
