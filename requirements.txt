fastapi==0.115.6
httpx==0.28.1
# transformers 4.53.3 requires huggingface-hub >= 0.30. Pin to 0.36.2 (well
# tested with both transformers 4.53.x and llama-cpp-python 0.3.x).
huggingface-hub==0.36.2
# Qwen3 GGUF needs a recent bundled llama.cpp; older 0.3.5 cannot load
# general.architecture=qwen3.
llama-cpp-python==0.3.23
pydantic==2.12.4
ray[serve]==2.55.1
torch==2.7.1
transformers==4.53.3
uvicorn==0.34.0
